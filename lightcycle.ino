/*
void lightcycle()
{
  int bike_x[4] = {0, screen_pixels_x / 2, screen_pixels_x, screen_pixels_x / 2}; //starting positions of the bikes starting with the player on the left of the screen
  int bike_y[4] = {screen_pixels_y / 2, 0, screen_pixels_y / 2, screen_pixels_y};
  
  byte road[128][16]; // the map that shows whether a pixel has been covered
  for(int i = 0; i < 127; i++)
  {
    for(int j = 0; j < 15; j++)
    {
      road[i][j] = 0;
    }
  }

}
*/
